ai_strategy_maintain_power_balance = {
	icon = "gfx/interface/icons/ai_strategy_icons/maintain_power_balance.dds"
	
	type = diplomatic
	
	will_form_power_bloc = {
		global_country_ranking = 1
	}
	
	undesirable_infamy_level = {
		value = 25
		
		if = {
			limit = { 
				has_technology_researched = pan-nationalism
			}
			multiply = 1.5
		}
		else_if = {
			limit = { 
				has_technology_researched = nationalism
			}
			multiply = 1.25
		}		
	}
	
	unacceptable_infamy_level = {
		value = 50
		
		if = {
			limit = { 
				has_technology_researched = pan-nationalism
			}
			multiply = 1.5
		}
		else_if = {
			limit = { 
				has_technology_researched = nationalism
			}
			multiply = 1.25
		}		
	}
	
	ideological_opinion_effect_mult = {
		value = 1.5
	}
	
	wargoal_maneuvers_fraction = {
		value = -0.1
	}	
	
	diplomatic_play_neutrality = {
		value = 0
	}
	
	diplomatic_play_boldness = {
		value = -25
	}
	
	recklessness = {
		value = -0.25
	}
	
	aggression = {
		value = 0.25	

		if = {
			limit = { 
				NOT = { is_country_type = unrecognized }
				scope:target_country = { is_country_type = unrecognized } 
				has_technology_researched = colonization
				institution_investment_level = {
					institution = institution_colonial_affairs
					value >= 1
				}					
			}
			add = 0.25
			
			if = {
				limit = {
					power_bloc ?= { has_principle = principle:principle_colonial_offices_3 }
				}
				add = 0.5
			}
			
			if = {
				limit = { has_technology_researched = civilizing_mission }
				add = 0.5
				
				if = {
					limit = { has_strategic_adjacency = scope:target_country }
					add = 0.5
				}
			}			
		}

		# Less aggressive against countries we're just antagonistic against, rather than belligerent/domineering
		if = {
			limit = {
				has_attitude = {
					who = scope:target_country
					attitude = antagonistic
				}					
			}
			multiply = 0.1
		}

		# More aggressive against ideological enemies, a bit less aggressive against ideological allies
		multiply = {
			value = "ai_ideological_opinion(scope:target_country)"
			multiply = -0.1
			add = 1
			max = 2.0
			min = 0.5
		}		
	}
	 
	building_group_weights = {
		bg_technology = 1.25
	}
	
	secret_goal_weights = {
		protect = 1.5
		dominate = 1.5
		reconcile = 1.5
	}
	
	wargoal_weights = {
		regime_change = 3.0
		humiliation = 2.0
		liberate_subject = 1.5
		liberate_country = 1.5
		force_nationalization = 0.25
	}

	possible = {
		is_subject = no
	}
	
	weight = {
		value = 10
		
		if = {
			limit = { country_rank < rank_value:major_power }
			add = 10
		}

	}
}

ai_strategy_territorial_expansion = {
	icon = "gfx/interface/icons/ai_strategy_icons/territorial_expansion.dds"
	
	type = diplomatic
	
	will_form_power_bloc = {
		OR = {
			global_country_ranking <= 3
			AND = {
				country_rank >= rank_value:great_power
				has_technology_researched = nationalism
			}
		}
	}
	
	undesirable_infamy_level = {
		value = 50
		
		if = {
			limit = { 
				has_technology_researched = pan-nationalism
			}
			multiply = 1.5
		}
		else_if = {
			limit = { 
				has_technology_researched = nationalism
			}
			multiply = 1.25
		}		
	}
	
	unacceptable_infamy_level = {
		value = 100
	}
	
	ideological_opinion_effect_mult = {
		value = 0.75
	}
	
	wargoal_maneuvers_fraction = {
		value = 0.15
	}		
	
	diplomatic_play_neutrality = {
		value = 25
	}
	
	diplomatic_play_boldness = {
		value = 25
	}
	
	recklessness = {
		value = 0.25
	}
	
	aggression = {
		value = 1
		
		if = {
			limit = { 
				NOT = { is_country_type = unrecognized }
				scope:target_country = { is_country_type = unrecognized } 
			}
			add = 1
		}	

		# Less aggressive against countries we're just antagonistic against, rather than belligerent/domineering
		if = {
			limit = {
				has_attitude = {
					who = scope:target_country
					attitude = antagonistic
				}					
			}
			multiply = 0.25
		}	

		# More aggressive against ideological enemies, a bit less aggressive against ideological allies
		multiply = {
			value = "ai_ideological_opinion(scope:target_country)"
			multiply = -0.1
			add = 1
			max = 2.0
			min = 0.5
		}			
	}

	building_group_weights = {
		bg_army = 1.5
	}
	
	secret_goal_weights = {
		protect = 0.5
		conquer = 1.5
	}

	wargoal_weights = {
		conquer_state = 1.5
		return_state = 1.5
		force_nationalization = 0.25
	}

	secret_goal_scores = {
		conquer = {
			if = {
				limit = {
					NOT = { scope:target_country = { is_subject_of = root } }
					can_reach_target_country = yes
					has_strategic_adjacency = scope:target_country
					scope:target_country.country_rank <= root.country_rank
				}
				
				if = {
					limit = {
						NOT = { is_country_type = unrecognized }
						scope:target_country = { NOT = { is_country_type = unrecognized } }
					}
					add = 25
				}		
				else_if = {
					limit = {				
						scope:target_country = { is_country_type = unrecognized }						
					}
					add = 50
				}
			}

			## Unless an AI has a specific strategy to conquer or dominate Dragon Isles, this will ensure they cannot roll conquer
			if = { 
				limit = {
					game_date < 1200.1.2
					NOR = {
						country_has_primary_culture = cu:illvasaari
						country_has_primary_culture = cu:kotskarvi
						country_has_primary_culture = cu:garagoz

					}
					scope:target_country = {
						OR = {
							country_has_primary_culture = cu:illvasaari
							country_has_primary_culture = cu:kotskarvi
							country_has_primary_culture = cu:garagoz
						}	
					}
				}
				multiply = 0
			}
		}
	}
		
	possible = {
		is_subject = no
	}

	weight = {
		value = 10
		# AI implications for Power Blocs: 'Territorial Expansion' strategy is more likely for Blocs with Aggressive Coordination
		if = {
			limit = {
				power_bloc ?= {
					has_principle_group = principle_group:principle_group_aggressive_coordination
				}
			}
			add = 5
		}
		if = {
			limit = { ig:ig_armed_forces ?= { is_powerful = yes } }
			multiply = 1.5
		}			
		if = {
			limit = { has_radical_leftist_government = yes }
			multiply = 0.5
		}
	}
}

ai_strategy_colonial_expansion = {
	icon = "gfx/interface/icons/ai_strategy_icons/colonial_expansion.dds"
	
	type = diplomatic
	
	will_form_power_bloc = {
		OR = {
			global_country_ranking <= 3
			AND = {
				country_rank >= rank_value:great_power
				has_technology_researched = nationalism
			}
		}
	}
	
	undesirable_infamy_level = {
		value = 25
		
		if = {
			limit = { 
				has_technology_researched = pan-nationalism
			}
			multiply = 1.5
		}
		else_if = {
			limit = { 
				has_technology_researched = nationalism
			}
			multiply = 1.25
		}		
	}
	
	unacceptable_infamy_level = {
		value = 50
		
		if = {
			limit = { 
				has_technology_researched = pan-nationalism
			}
			multiply = 1.5
		}
		else_if = {
			limit = { 
				has_technology_researched = nationalism
			}
			multiply = 1.25
		}		
	}
	
	ideological_opinion_effect_mult = {
		value = 1.0
	}
	
	wargoal_maneuvers_fraction = {
		value = 0
	
		if = {
			limit = {
				scope:enemy_country = { is_country_type = unrecognized }
			}
			add = 0.25
		}	
	}		
	
	diplomatic_play_neutrality = {
		value = 25
	}
	
	diplomatic_play_boldness = {
		value = -15
	}
	
	recklessness = {
		value = 0.1
	}
	
	# Added to value from default strategy
	aggression = {
		value = 0.25
		if = {
			limit = { 
				NOT = { is_country_type = unrecognized }
				scope:target_country = { is_country_type = unrecognized }
				has_technology_researched = colonization
				institution_investment_level = {
					institution = institution_colonial_affairs
					value >= 1
				}					
			}
			add = 0.25

			if = {
				limit = {
					power_bloc ?= { has_principle = principle:principle_colonial_offices_3 }
				}
				add = 0.75
			}			
			
			if = {
				limit = { has_technology_researched = civilizing_mission }
				add = 1.5
				
				if = {
					limit = { has_strategic_adjacency = scope:target_country }
					add = 1
				}

				if = {
					limit = { has_technology_researched = malaria_prevention }
					add = 1
				}		
			}			

			if = {
				limit = {
					scope:target_country = {
						is_subject = yes
						is_country_type = unrecognized
						top_overlord ?= {
							NOT = { is_country_type = unrecognized }
						}
					}
				}
				multiply = 0.0
			}			
		}

		# Less aggressive against countries we're just antagonistic against, rather than belligerent/domineering
		if = {
			limit = {
				has_attitude = {
					who = scope:target_country
					attitude = antagonistic
				}					
			}
			multiply = 0.1
		}	

		# More aggressive against ideological enemies, a bit less aggressive against ideological allies
		multiply = {
			value = "ai_ideological_opinion(scope:target_country)"
			multiply = -0.1
			add = 1
			max = 2.0
			min = 0.5
		}	
	}
	
	building_group_weights = {
		bg_navy = 1.5
	}
	
	colonial_interest_ratio = {
		value = 0.10
	
		if = {
			limit = { navy_size >= 30 }
			add = 0.1
		}
		
		if = {
			limit = { navy_size >= 60 }
			add = 0.1
		}		
		
		if = {
			limit = { navy_size >= 90 }
			add = 0.1
		}	
		
		if = {
			limit = { navy_size >= 120 }
			add = 0.1
		}			
	}
	
	secret_goal_scores = {
		dominate = {
			if = {
				limit = {
					scope:target_country = { NOT = { is_country_type = unrecognized } }
				}
				add = -100
			}		
			if = {
				limit = {
					NOT = { scope:target_country = { is_subject_of = root } }
					has_strategic_adjacency = scope:target_country
					scope:target_country = { is_country_type = unrecognized }
				}
				add = 200
			}	
		}		
		conquer = {
			if = {
				limit = {
					scope:target_country = { NOT = { is_country_type = unrecognized } }
				}
				add = -100
			}		
			if = {
				limit = {
					NOT = { scope:target_country = { is_subject_of = root } }
					has_strategic_adjacency = scope:target_country
					scope:target_country = { is_country_type = unrecognized }
				}
				add = 200
			}	
		}		
	}

	wargoal_weights = {
		conquer_state = 1.5
		reduce_autonomy = 1.5
		take_treaty_port = 1.5
		force_nationalization = 0.5
	}	
		
	possible = {
		is_subject = no
		is_country_type = recognized
		OR = {
			has_law = law_type:law_colonial_resettlement
			has_law = law_type:law_colonial_exploitation
		}
	}

	weight = {
		value = 10

		if = {
			limit = { country_rank = rank_value:major_power }
			add = 5
		}	

		if = {
			limit = { country_rank >= rank_value:great_power }
			add = 5
		}			
		
		# AI implications for Power Blocs: 'Colonial Expansion' strategy is more likely for Blocs with Colonial Offices
		if = {
			limit = {
				power_bloc ?= {
					has_principle_group = principle_group:principle_group_colonial_offices
				}
			}
			add = 5
		}

		if = {
			limit = { navy_size < 50 }
			multiply = 0.5
		}		
		
		if = {
			limit = { navy_size >= 75 }
			multiply = 1.5
		}	

		if = {
			limit = { has_technology_researched = civilizing_mission }
			multiply = 1.5
		}	
		
		if = {
			limit = {
				power_bloc ?= { has_principle_group = principle_group:principle_group_colonial_offices }
			}
			multiply = 1.5
		}		
		
		if = {
			limit = { has_radical_leftist_government = yes }
			multiply = 0.01
		}			
	}
}

ai_strategy_armed_isolationism = {
	icon = "gfx/interface/icons/ai_strategy_icons/armed_isolationism.dds"
	
	type = diplomatic

	undesirable_infamy_level = {
		value = 25
		
		if = {
			limit = { 
				has_technology_researched = pan-nationalism
			}
			multiply = 1.5
		}
		else_if = {
			limit = { 
				has_technology_researched = nationalism
			}
			multiply = 1.25
		}		
	}
	
	unacceptable_infamy_level = {
		value = 50
		
		if = {
			limit = { 
				has_technology_researched = pan-nationalism
			}
			multiply = 1.5
		}
		else_if = {
			limit = { 
				has_technology_researched = nationalism
			}
			multiply = 1.25
		}		
	}
	
	ideological_opinion_effect_mult = {
		value = 0.75
	}
	
	wargoal_maneuvers_fraction = {
		value = -0.1
	}		
	
	diplomatic_play_neutrality = {
		value = 50
	}
	
	diplomatic_play_boldness = {
		value = 0
	}
	
	recklessness = {
		value = -0.5
	}
	
	aggression = {
		value = 0
	}
	
	building_group_weights = {
		bg_army = 1.5
	}
	
	secret_goal_weights = {
		dominate = 0.0
		protect = 0.0
	}

	wargoal_weights = {
		conquer_state = 0.0
		make_protectorate = 0.0
		reduce_autonomy = 0.0
		make_tributary = 0.0
		force_nationalization = 2.0
	}	
	
	possible = {
		is_subject = no
		NOT = { country_rank >= rank_value:great_power }
		NOT = { has_radical_leftist_government = yes }
		is_major_unifier_candidate = no
	}
	
	weight = {
		value = 10
		
		if = {
			limit = { is_country_type = unrecognized }
			add = 50
		}
		
		if = {
			limit = { has_law = law_type:law_national_militia }
			add = 50
		}

		if = {
			limit = { has_law = law_type:law_isolationism }
			add = 100
		}	

		if = {
			limit = { country_rank >= rank_value:great_power }
			multiply = 0.1
		}			
		
		if = {
			limit = { country_rank = rank_value:major_power }
			multiply = 0.5
		}		
	}
}

ai_strategy_economic_imperialism = {
	icon = "gfx/interface/icons/ai_strategy_icons/economic_imperialism.dds"
	
	type = diplomatic
	
	will_form_power_bloc = {
		OR = {
			global_country_ranking <= 3
			AND = {
				country_rank >= rank_value:great_power
				has_technology_researched = nationalism
			}
		}
	}	
	
	undesirable_infamy_level = {
		value = 25
		
		if = {
			limit = { 
				has_technology_researched = pan-nationalism
			}
			multiply = 1.5
		}
		else_if = {
			limit = { 
				has_technology_researched = nationalism
			}
			multiply = 1.25
		}		
	}
	
	unacceptable_infamy_level = {
		value = 50
		
		if = {
			limit = { 
				has_technology_researched = pan-nationalism
			}
			multiply = 1.5
		}
		else_if = {
			limit = { 
				has_technology_researched = nationalism
			}
			multiply = 1.25
		}	
	}
	
	ideological_opinion_effect_mult = {
		value = 1.0
	}
	
	wargoal_maneuvers_fraction = {
		value = 0
	
		if = {
			limit = {
				scope:enemy_country = { is_country_type = unrecognized }
			}
			add = 0.25
		}
	}
	
	obligation_value = {
		if = { # Countries with Economic Imperialism is always looking to bring more countries into their market
			limit = {
				scope:target_country.country_rank < country_rank
				NOR = {
					scope:target_country = { is_subject = yes }
					is_in_customs_union_with = scope:target_country
				}
			}
			add = 5
		}
	}
	
	diplomatic_play_neutrality = {
		value = 10
	}
	
	diplomatic_play_boldness = {
		value = -10
	}
	
	recklessness = {
		value = -0.1
	}
	
	aggression = {
		value = 0.5
		if = {
			limit = { 
				NOT = { is_country_type = unrecognized }
				scope:target_country = { is_country_type = unrecognized } 
				has_technology_researched = colonization
				institution_investment_level = {
					institution = institution_colonial_affairs
					value >= 1
				}					
			}
			add = 0.5
					
			if = {
				limit = {
					power_bloc ?= { has_principle = principle:principle_colonial_offices_3 }
				}
				add = 0.5
			}			
			
			if = {
				limit = { has_technology_researched = civilizing_mission }
				add = 0.5
				
				if = {
					limit = { has_strategic_adjacency = scope:target_country }
					add = 0.5
				}				
			}

			if = {
				limit = {
					scope:target_country = {
						is_subject = yes
						is_country_type = unrecognized
						top_overlord ?= {
							NOT = { is_country_type = unrecognized }
						}
					}
				}
				multiply = 0.1
			}		
		}

		if = {
			limit = {
				scope:target_country = {
					is_subject = yes
					top_overlord ?= {
						NOT = { is_country_type = unrecognized }
					}
				}
			}
			multiply = 0.1
		}		

		# Less aggressive against countries we're just antagonistic against, rather than belligerent/domineering
		if = {
			limit = {
				has_attitude = {
					who = scope:target_country
					attitude = antagonistic
				}					
			}
			multiply = 0.25
		}

		# More aggressive against ideological enemies, a bit less aggressive against ideological allies
		multiply = {
			value = "ai_ideological_opinion(scope:target_country)"
			multiply = -0.1
			add = 1
			max = 2.0
			min = 0.5
		}			
	}
	
	building_group_weights = {
		bg_navy = 1.5
	}
	
	colonial_interest_ratio = {
		value = 0.10
	
		if = {
			limit = { navy_size >= 30 }
			add = 0.05
		}
		
		if = {
			limit = { navy_size >= 60 }
			add = 0.05
		}		
		
		if = {
			limit = { navy_size >= 90 }
			add = 0.05
		}	
		
		if = {
			limit = { navy_size >= 120 }
			add = 0.05
		}			
	}
	
	strategic_region_scores = {

	}
	
	secret_goal_scores = {
		dominate = {	
	
			if = {
				limit = {
					NOT = { scope:target_country = { is_subject_of = root } }
					has_strategic_adjacency = scope:target_country
					root.country_rank > scope:target_country.country_rank
					scope:target_country.country_rank < rank_value:major_power
					scope:target_country = {
						NOR = { 
							has_law = law_type:law_free_trade 
							has_law = law_type:law_isolationism
						}
					}
				}
				add = 50
				
				if = {
					limit = {
						NOT = { scope:target_country = { is_subject_of = root } }
						scope:target_country = { is_country_type = unrecognized }
					}
					add = 50
				}
			}		
		}		
		conquer = {		
			if = {
				limit = {
					has_strategic_adjacency = scope:target_country
					scope:target_country = { is_country_type = unrecognized }
				}
				add = 50
			}
			if = {
				limit = {
					scope:target_country = { NOT = { is_country_type = unrecognized } }
				}
				add = -50
			}
				
		}		
	}

	wargoal_weights = {
		make_protectorate = 1.5	
		make_tributary = 1.5
		make_dominion = 1.5
		force_nationalization = 1.5		
	}	
	
	treaty_port_value = {
		if = {
			limit = {
				can_reach_target_state = yes
				scope:target_country = {
					has_law = law_type:law_isolationism
					gdp >= 2000000 # Not worth doing against very small markets
				}
				scope:target_state = { is_incorporated = yes }
			}
			add = {
				value = 50
			
				# More likely to grab a treaty port if we're already enemies in a play, ie they attacked us first
				if = {
					limit = { is_diplomatic_play_enemy_of = scope:target_country }
					multiply = 2.0
				}
			}
		}	
	}
	
	wargoal_scores = {	
		# Need to have identical weights for foreign_investment_rights and open_market here as which one is used depends on whether Foreign Investment is available
		foreign_investment_rights = {
			value = 0
		
			if = {
				limit = {
					country_rank >= rank_value:great_power
					can_reach_target_country = yes
					scope:target_country = {
						has_law = law_type:law_isolationism
						gdp >= 5000000 # Not worth doing against small markets
					}
				}
				add = {
					value = 100
				
					# Less interested in foreign investment rights as a sway or defensive wargoal
					if = {
						limit = { is_diplomatic_play_involved_with = scope:target_country }
						multiply = 0.2
					}
				}
			}
		}		
		open_market = {
			value = 0
		
			if = {
				limit = {
					country_rank >= rank_value:great_power
					can_reach_target_country = yes
					scope:target_country = {
						has_law = law_type:law_isolationism
						gdp >= 5000000 # Not worth doing against small markets
					}
				}
				add = {
					value = 100
				
					# Less interested in open market as a sway or defensive wargoal
					if = {
						limit = { is_diplomatic_play_involved_with = scope:target_country }
						multiply = 0.2
					}
				}
			}
		}		
	}
		
	possible = {
		is_subject = no
		navy_size >= 25
		is_country_type = recognized
		NOT = { has_law = law_type:law_isolationism }
		OR = {
			country_rank = rank_value:major_power
			country_rank >= rank_value:great_power
		}	
	}

	weight = {
		value = 10		

		if = {
			limit = { country_rank >= rank_value:great_power }
			add = 10
		}

		# AI implications for Power Blocs: 'Economic Imperialism' strategy is more likely for Blocs with Market Unification
		if = {
			limit = {
				power_bloc ?= {
					has_principle_group = principle_group:principle_group_market_unification
				}
			}
			add = 5
		}

		if = {
			limit = { navy_size < 50 }
			multiply = 0.5
		}		
		
		if = {
			limit = { navy_size >= 75 }
			multiply = 1.5
		}	
		
		if = {
			limit = { has_radical_leftist_government = yes }
			multiply = 0.25
		}
	}
}

ai_strategy_socialism_in_one_country = {
	icon = "gfx/interface/icons/ai_strategy_icons/armed_isolationism.dds"
	
	type = diplomatic

	undesirable_infamy_level = {
		value = 25
		
		if = {
			limit = { 
				has_technology_researched = pan-nationalism
			}
			multiply = 1.5
		}
		else_if = {
			limit = { 
				has_technology_researched = nationalism
			}
			multiply = 1.25
		}			
	}
	
	unacceptable_infamy_level = {
		value = 50
		
		if = {
			limit = { 
				has_technology_researched = pan-nationalism
			}
			multiply = 1.5
		}
		else_if = {
			limit = { 
				has_technology_researched = nationalism
			}
			multiply = 1.25
		}			
	}
	
	ideological_opinion_effect_mult = {
		value = 0.75
	}
	
	wargoal_maneuvers_fraction = {
		value = -0.1
	}		
	
	diplomatic_play_neutrality = {
		value = 50
	}
	
	diplomatic_play_boldness = {
		value = 0
	}
	
	recklessness = {
		value = -0.5
	}
	
	aggression = {
		value = 0
	}
	
	building_group_weights = {
		bg_army = 1.25
		bg_technology = 1.25
	}
	
	secret_goal_scores = {
		protect = {
			if = {
				limit = {
					scope:target_country = { has_radical_leftist_government = yes }
				}
				multiply = 1.5
			}		
			if = {
				limit = {
					has_strategic_adjacency = scope:target_country
					scope:target_country = { has_radical_leftist_government = yes }
				}
				add = 200
			}	
		}		
		befriend = {		
			if = {
				limit = {
					NOT = { scope:target_country = { is_subject_of = root } }
					scope:target_country = { has_radical_leftist_government = yes }
				}
				add = 200
			}	
		}
	}

	wargoal_weights = {
		conquer_state = 0.0
		make_protectorate = 0.0
		reduce_autonomy = 0.0
		make_tributary = 0.0
		make_dominion = 0.0
		force_nationalization = 5.0
	}	
	
	possible = {
		is_subject = no
		has_radical_leftist_government = yes
	}
	
	weight = {
		value = 50
		
		if = {
			limit = { is_country_type = unrecognized }
			add = 50
		}
		
		if = {
			limit = { has_law = law_type:law_national_militia }
			add = 50
		}

		if = {
			limit = { has_law = law_type:law_isolationism }
			add = 200
		}	

		if = {
			limit = { country_rank >= rank_value:great_power }
			multiply = 0.25
		}			
		
		if = {
			limit = { country_rank = rank_value:major_power }
			multiply = 0.5
		}	
		
		if = {
			limit = { 
				any_country = {
					NOT = {
						THIS = ROOT
					}
					has_strategic_adjacency = ROOT
					has_radical_leftist_government = yes
					country_rank >= rank_value:great_power
				}
			}
			multiply = 0.05
		}		
		else_if = {
			limit = { 
				any_country = {
					NOT = {
						THIS = ROOT
					}
					has_radical_leftist_government = yes
					country_rank >= rank_value:great_power
				}
			}
			multiply = 0.25
		}
		else = {
			multiply = 2
		}
	}
}

ai_strategy_world_revolution = {
	icon = "gfx/interface/icons/ai_strategy_icons/revolutionary.dds"
	
	type = diplomatic
		
	will_form_power_bloc = {
		always = yes
	}			
	
	undesirable_infamy_level = {
		value = 75
	}
	
	unacceptable_infamy_level = {
		value = 100
	}
	
	ideological_opinion_effect_mult = {
		value = 3
	}
	
	wargoal_maneuvers_fraction = {
		value = 0.25
	}		
	
	diplomatic_play_neutrality = {
		value = 0
	}
	
	diplomatic_play_boldness = {
		value = 75
	}
	
	recklessness = {
		value = 0.5
	}
	
	aggression = {
		value = 1
		
		if = {
			limit = { 
				scope:target_country = {
					has_radical_leftist_government = no
				} 
			}
			add = 3
		}	

		if = {
			limit = {
				scope:target_country = {
					has_radical_leftist_government = yes
				}		
			}
			multiply = 0.25
		}	

		# More aggressive against ideological enemies, a bit less aggressive against ideological allies
		multiply = {
			value = "ai_ideological_opinion(scope:target_country)"
			multiply = -0.1
			add = 1
			max = 2.0
			min = 0.5
		}			
	}

	building_group_weights = {
		bg_army = 1.5
	}
	
	secret_goal_scores = {
		protect = {
			if = {
				limit = {
					scope:target_country = {
						OR = {
							has_radical_leftist_government = yes
							has_radical_leftist_economy = yes
						}					
					}
				}
				add = 100
			}		
			if = {
				limit = {
					has_strategic_adjacency = scope:target_country
					scope:target_country = { has_radical_leftist_government = yes }
				}
				add = 200
			}	
		}		
		befriend = {		
			if = {
				limit = {
					scope:target_country = { has_radical_leftist_government = yes }
				}
				add = 200
			}	
		}
		dominate = {
			if = {
				limit = {
					NOT = { scope:target_country = { is_subject_of = root } }
					has_strategic_adjacency = scope:target_country
					scope:target_country = { NOT = { has_radical_leftist_government = yes } }
				}
				add = 100
			}
			if = {
				limit = {
					NOT = { scope:target_country = { is_subject_of = root } }
					has_strategic_adjacency = scope:target_country
					scope:target_country = { country_rank < root.country_rank }
				}
				multiply = 1.5
			}
		}
		conquer = {
			if = {
				limit = {
					has_strategic_adjacency = scope:target_country
					scope:target_country = { NOT = { has_radical_leftist_government = yes } }
				}
				add = 50
			}
		}
	}

	wargoal_weights = {
		regime_change = 3.0
		force_nationalization = 1.0
	}	
		
	possible = {
		is_subject = no
		has_radical_leftist_government = yes
	}

	weight = {
		value = 25
		
		if = {
			limit = { 
				OR = {
					has_law = law_type:law_professional_army 
					has_law = law_type:law_mass_conscription 
				}
			}
			add = 25
		}

		if = {
			limit = { has_law = law_type:law_isolationism }
			add = -200
		}	

		if = {
			limit = { country_rank >= rank_value:great_power }
			multiply = 1.25
		}			
		
		if = {
			limit = { country_rank = rank_value:major_power }
			multiply = 1.10
		}	
		
		if = {
			limit = { 
				any_country = {
					NOT = {
						THIS = ROOT
					}
					has_strategic_adjacency = ROOT
					has_radical_leftist_government = yes
					country_rank >= rank_value:great_power
				}
			}
			multiply = 1.5
		}		
		else_if = {
			limit = { 
				any_country = {
					NOT = {
						THIS = ROOT
					}
					has_radical_leftist_government = yes
					country_rank >= rank_value:great_power
				}
			}
			multiply = 1.15
		}
	}
}

ai_strategy_anti_imperialism = {
	icon = "gfx/interface/icons/ai_strategy_icons/protect_region.dds"
	
	type = diplomatic
		
	will_form_power_bloc = {
		country_rank >= rank_value:great_power
	}			
	
	undesirable_infamy_level = {
		value = 25
		
		if = {
			limit = { 
				has_technology_researched = pan-nationalism
			}
			multiply = 1.5
		}
		else_if = {
			limit = { 
				has_technology_researched = nationalism
			}
			multiply = 1.25
		}		
	}
	
	unacceptable_infamy_level = {
		value = 50
		
		if = {
			limit = { 
				has_technology_researched = pan-nationalism
			}
			multiply = 1.5
		}
		else_if = {
			limit = { 
				has_technology_researched = nationalism
			}
			multiply = 1.25
		}			
	}
	
	ideological_opinion_effect_mult = {
		value = 1.5
	}
	
	wargoal_maneuvers_fraction = {
		value = -0.1
	}	
	
	diplomatic_play_neutrality = {
		value = 0
	}
	
	diplomatic_play_boldness = {
		value = -25
	}
	
	recklessness = {
		value = -0.25
	}
	
	aggression = {
		value = 0.25	

		if = {
			limit = {
				scope:target_country = {
					is_diplomatic_play_initiator = yes
					scope:target_country = { is_country_type = recognized } 
					any_enemy_in_diplo_play = {
						is_country_type = unrecognized
					}
				}
			}
			add = 1
		}

		if = {
			limit = { 
				scope:target_country = { NOT = { has_radical_leftist_government = yes } } 
			}
			multiply = 2
		}

		# Less aggressive against countries we're just antagonistic against, rather than belligerent/domineering
		if = {
			limit = {
				has_attitude = {
					who = scope:target_country
					attitude = antagonistic
				}					
			}
			multiply = 0.1
		}

		# Less aggressive against unrecognized powers
		if = {
			limit = {
				scope:target_country = { 
					is_country_type = unrecognized
					NOT = {
						relations:root <= relations_threshold:poor
					}		 
				}	
			}
			multiply = 0.25
		}

		# More aggressive against colonial powers

		if = {
			limit = {
				scope:target_country = { 
					is_country_type = colonial 
				}	
			}
			multiply = 2
		}

		# AI implications for Power Blocs: Generally more aggressive against other Bloc leaders with Exploitation of Members
		if = {
			limit = {
				scope:target_country = {
					is_power_bloc_leader = yes
					NOT = {
						is_in_same_power_bloc = root
					}
					power_bloc ?= {
						has_principle_group = principle_group:principle_group_exploit_members
					}
				}
			}
			multiply = 5
		}

		# More aggressive against ideological enemies, a bit less aggressive against ideological allies
		multiply = {
			value = "ai_ideological_opinion(scope:target_country)"
			multiply = -0.1
			add = 1
			max = 2.0
			min = 0.5
		}		
	}
	 
	building_group_weights = {
		bg_technology = 1.25
	}
	
	secret_goal_scores = {
		protect = {		
			if = {
				limit = {
					scope:target_country = { has_radical_leftist_government = yes }
				}
				add = 50
			}		
			if = {
				limit = {
					scope:target_country = { is_country_type = unrecognized }
				}
				add = 50
			}	
		}		
		befriend = {		
			if = {
				limit = {
					NOT = { scope:target_country = { is_subject_of = root } }
					scope:target_country = { has_radical_leftist_government = yes }
				}
				add = 100
			}	
			if = {
				limit = {
					NOT = { scope:target_country = { is_subject_of = root } }
					scope:target_country = { is_country_type = unrecognized }
				}
				add = 50
			}
		}
		antagonize = {
			if = {
				limit = {
					NOT = { scope:target_country = { is_subject_of = root } }
					has_strategic_adjacency = scope:target_country
					scope:target_country = { 
						OR = { 
							has_strategy = ai_strategy_economic_imperialism 
							has_strategy = ai_strategy_territorial_expansion 
						}
					}
				}
				add = 50
			}
			if = {
				limit = {
					NOT = { scope:target_country = { is_subject_of = root } }
					is_country_type = colonial
				}
				add = 25
			}
		}
	}
	
	wargoal_weights = {
		regime_change = 1.5
		humiliation = 1.5
		liberate_subject = 5.0
		liberate_country = 3.0
		force_nationalization = 2.0
	}

	possible = {
		is_subject = no
		has_radical_leftist_government = yes
	}
	
	weight = {
		value = 10
		
		if = {
			limit = { country_rank >= rank_value:major_power }
			add = 25
		}
	}
}

ai_strategy_vampiric_expansion = {
	icon = "gfx/interface/icons/ai_strategy_icons/necromantic_agenda.dds"
	
	type = diplomatic
	
	will_form_power_bloc = {
		OR = {
			global_country_ranking <= 3
			AND = {
				country_rank >= rank_value:great_power
				has_technology_researched = nationalism
			}
		}
	}
	
	undesirable_infamy_level = {
		value = 50
	}
	
	unacceptable_infamy_level = {
		value = 100
	}
	
	ideological_opinion_effect_mult = {
		value = 0.75
	}
	
	wargoal_maneuvers_fraction = {
		value = 0.15
	}		
	
	diplomatic_play_neutrality = {
		value = 25
	}
	
	diplomatic_play_boldness = {
		value = 25
	}
	
	recklessness = {
		value = 0.25

		if = {
			limit = {
				scope:target_country = { country_rank < rank_value:major_power } 
			}
			add = 0.75
		}	
	}
	
	aggression = {
		value = 0

		if = {
			limit = {
				scope:target_country = { country_rank < rank_value:major_power } 
			}
			add = 10
		}	 
				
	}

	building_group_weights = {
		bg_army = 100
	}
	
	secret_goal_weights = {
		protect = 0.5
		dominate = 1.5
		conquer = 1.5
	}

	wargoal_weights = {
		conquer_state = 1.5
		return_state = 1.5
		make_protectorate = 1.5
	}

	secret_goal_scores = {
		conquer = {
			if = {
				limit = {
					NOT = { scope:target_country = { is_subject_of = root } }
					can_reach_target_country = yes
					has_strategic_adjacency = scope:target_country
					scope:target_country.country_rank < rank_value:major_power
				}
				add = 50
				
			}
			if = {
				limit = {				
					scope:target_country = { is_country_type = unrecognized }						
				}
				add = 50
			}
		}
	}
		
	possible = {
		c:RVC ?= this
	}

	weight = {
		value = 1000
	}
}

### UNIFICATION STRATEGIES ###

ai_strategy_unify_thaloria = {
	icon = "gfx/interface/icons/ai_strategy_icons/unification.dds"
	
	type = diplomatic
	
	will_form_power_bloc = {
		always = yes
	}
	
	undesirable_infamy_level = {
		value = 90
	}
	
	unacceptable_infamy_level = {
		value = 100
	}
	
	ideological_opinion_effect_mult = {
		value = 0.5
	}
	
	wargoal_maneuvers_fraction = {
		value = 0.2
	}		
	
	diplomatic_play_neutrality = {
		value = 0
	}
	
	diplomatic_play_boldness = {
		value = 75
	}

	obligation_value = {
		value = 0
	
		if = {
			limit = { 
				scope:target_country = {			
					OR = {
					country_has_primary_culture = cu:lake_thalorian
					country_has_primary_culture = cu:littoral_thalorian
					country_has_primary_culture = cu:heartland_thalorian
					country_has_primary_culture = cu:gnome_thalorian
					}
				}
			}
			add = 25
		}
	}	

	recklessness = {
		value = 0.5
	}
	
	aggression = {
		value = 0.25
		
		if = {
			limit = { 
				scope:target_country = {			
					OR = {
					country_has_primary_culture = cu:lake_thalorian
					country_has_primary_culture = cu:littoral_thalorian
					country_has_primary_culture = cu:heartland_thalorian
					country_has_primary_culture = cu:gnome_thalorian
					}
				}
			}
			add = 2.75
		}

	}

	building_group_weights = {
		bg_army = 1.5
	}
	
	state_value = {
		if = {
			limit = {
				scope:target_state = {
					state_region = {
						OR = {
						is_homeland = cu:lake_thalorian
						is_homeland = cu:littoral_thalorian
						is_homeland = cu:heartland_thalorian
						is_homeland = cu:gnome_thalorian
						}
					}
				}
				can_reach_target_state = yes					
			}
			add = 100
		}
	}
	
	wargoal_scores = {	
		unification_leadership = {
			value = 1000
		}	
	}
	
	secret_goal_scores = {
		protect = {		
			if = {
				limit = {
					scope:target_country = {
						OR = {
							country_has_primary_culture = cu:lake_thalorian
							country_has_primary_culture = cu:littoral_thalorian
							country_has_primary_culture = cu:heartland_thalorian
							country_has_primary_culture = cu:gnome_thalorian
						}
						NOT = { is_unification_candidate = TLA }
						OR = { 
							is_supporting_unification_candidate = { who = root country_formation = TLA }
							is_subject_of = root
							is_in_customs_union_with = root
						}						
					}
				}
				add = 1000
			}		
		}		
		dominate = {		
			if = {
				limit = {
					scope:target_country = {
						OR = {
							country_has_primary_culture = cu:lake_thalorian
							country_has_primary_culture = cu:littoral_thalorian
							country_has_primary_culture = cu:heartland_thalorian
							country_has_primary_culture = cu:gnome_thalorian
						}					
						NOT = { is_unification_candidate = TLA }
						NOR = { 
							is_supporting_unification_candidate = { who = root country_formation = TLA }
							is_subject_of = root
							is_in_customs_union_with = root
						}	
					}
				}
				add = 1000
			}		
		}
		conquer = {				
			if = {
				limit = {
					scope:target_country = {
						OR = {
							country_has_primary_culture = cu:lake_thalorian
							country_has_primary_culture = cu:littoral_thalorian
							country_has_primary_culture = cu:heartland_thalorian
							country_has_primary_culture = cu:gnome_thalorian
						}
						NOR = { 
							is_subject_of = root
							is_in_customs_union_with = root
						}						
					}
					can_reach_target_country = yes
				}
				add = 500
			}				
		}
	}	

	possible = {
		is_subject = no
		can_form_nation = TLA
		NOT = { country_tier = empire }
	}

	weight = {
		value = 1000
	}
}

ai_strategy_unify_khus = {
	icon = "gfx/interface/icons/ai_strategy_icons/unification.dds"
	
	type = diplomatic
	
	will_form_power_bloc = {
		always = yes
	}
	
	undesirable_infamy_level = {
		value = 90
	}
	
	unacceptable_infamy_level = {
		value = 100
	}
	
	ideological_opinion_effect_mult = {
		value = 0.5
	}
	
	wargoal_maneuvers_fraction = {
		value = 0.2
	}		
	
	diplomatic_play_neutrality = {
		value = 0
	}
	
	diplomatic_play_boldness = {
		value = 75
	}

	obligation_value = {
		value = 0
	
		if = {
			limit = { 
				scope:target_country = {			
					OR = {
						country_has_primary_culture = cu:azmaraan
						country_has_primary_culture = cu:qinnaat
					}
				}
			}
			add = 25
		}
	}	

	recklessness = {
		value = 0.5
	}
	
	aggression = {
		value = 0.25

		if = {
			limit = { 
				scope:target_country = {			
					OR = {
						country_has_primary_culture = cu:azmaraan
						country_has_primary_culture = cu:qinnaat
					}
				}
			}
			add = 2.75
		}		

	}

	building_group_weights = {
		bg_army = 1.5
	}
	
	state_value = {
		if = {
			limit = {
				scope:target_state = {
					state_region = {
						OR = {
						is_homeland = cu:azmaraan
						is_homeland = cu:qinnaat
						}
					}
				}
				can_reach_target_state = yes					
			}
			add = 100
		}
	}
	
	wargoal_scores = {	
		unification_leadership = {
			value = 1000
		}	
	}
	
	secret_goal_scores = {
		protect = {		
			if = {
				limit = {
					scope:target_country = {
						OR = {
							country_has_primary_culture = cu:azmaraan
							country_has_primary_culture = cu:qinnaat
						}
						NOT = { is_unification_candidate = NEK }
						OR = { 
							is_supporting_unification_candidate = { who = root country_formation = NEK }
							is_subject_of = root
							is_in_customs_union_with = root
						}						
					}
				}
				add = 1000
			}		
		}		
		dominate = {		
			if = {
				limit = {
					scope:target_country = {
						OR = {
							country_has_primary_culture = cu:azmaraan
							country_has_primary_culture = cu:qinnaat
						}
						NOT = { is_unification_candidate = NEK }
						OR = { 
							is_supporting_unification_candidate = { who = root country_formation = NEK }
							is_subject_of = root
							is_in_customs_union_with = root
						}						
					}
				}
				add = 1000
			}		
		}
		conquer = {				
			if = {
				limit = {
					scope:target_country = {
						OR = {
							country_has_primary_culture = cu:azmaraan
							country_has_primary_culture = cu:qinnaat
						}
						NOR = { 
							is_subject_of = root
							is_in_customs_union_with = root
						}						
					}
					can_reach_target_country = yes
				}
				add = 500
			}				
		}
	}	

	possible = {
		is_subject = no
		can_form_nation = NEK	
		NOT = { country_tier = empire }
	}

	weight = {
		value = 1000
	}
}

ai_strategy_unify_gavkhan = {
	icon = "gfx/interface/icons/ai_strategy_icons/unification.dds"
	
	type = diplomatic
	
	will_form_power_bloc = {
		always = yes
	}
	
	undesirable_infamy_level = {
		value = 90
	}
	
	unacceptable_infamy_level = {
		value = 100
	}
	
	ideological_opinion_effect_mult = {
		value = 0.5
	}
	
	wargoal_maneuvers_fraction = {
		value = 0.2
	}		
	
	diplomatic_play_neutrality = {
		value = 0
	}
	
	diplomatic_play_boldness = {
		value = 75
	}

	obligation_value = {
		value = 0
	
		if = {
			limit = { 
				scope:target_country = {			
					OR = {
						country_has_primary_culture = cu:varbat
						country_has_primary_culture = cu:xhurlob
						country_has_primary_culture = cu:rethrashar
					}	
				}
			}
			add = 25
		}
	}	

	recklessness = {
		value = 0.5

		if = {
			limit = { 
				scope:target_country = {			
					OR = {
						country_has_primary_culture = cu:varbat
						country_has_primary_culture = cu:xhurlob
						country_has_primary_culture = cu:rethrashar
					}	
				}
			}
			add = 2
		}

	}
	
	aggression = {
		value = 0.25
		
		if = {
			limit = { 
				scope:target_country = {			
					OR = {
						country_has_primary_culture = cu:varbat
						country_has_primary_culture = cu:xhurlob
						country_has_primary_culture = cu:rethrashar
					}	
				}
			}
			add = 2.75
		}

	}

	building_group_weights = {
		bg_army = 1.5
	}
	
	state_value = {
		if = {
			limit = {
				scope:target_state = {
					state_region = {
						OR = {
						is_homeland = cu:varbat
						is_homeland = cu:xhurlob
						is_homeland = cu:rethrashar
						}
					}
				}
				can_reach_target_state = yes					
			}
			add = 100
		}
	}
	
	wargoal_scores = {	
		unification_leadership = {
			value = 1000
		}	
	}
	
	secret_goal_scores = {
		protect = {		
			if = {
				limit = {
					scope:target_country = {
						OR = {
							country_has_primary_culture = cu:varbat
							country_has_primary_culture = cu:xhurlob
							country_has_primary_culture = cu:rethrashar
						}	
						NOT = { is_unification_candidate = GAV }
						OR = { 
							is_supporting_unification_candidate = { who = root country_formation = GAV }
							is_subject_of = root
							is_in_customs_union_with = root
						}						
					}
				}
				add = 1000
			}		
		}		
		dominate = {		
			if = {
				limit = {
					scope:target_country = {
						OR = {
							country_has_primary_culture = cu:varbat
							country_has_primary_culture = cu:xhurlob
							country_has_primary_culture = cu:rethrashar
						}	
						NOT = { is_unification_candidate = GAV }
						OR = { 
							is_supporting_unification_candidate = { who = root country_formation = GAV }
							is_subject_of = root
							is_in_customs_union_with = root
						}						
					}
				}
				add = 1000
			}		
		}
		conquer = {				
			if = {
				limit = {
					scope:target_country = {
						OR = {
							country_has_primary_culture = cu:varbat
							country_has_primary_culture = cu:xhurlob
							country_has_primary_culture = cu:rethrashar
						}
						NOR = { 
							is_subject_of = root
							is_in_customs_union_with = root
						}						
					}
					can_reach_target_country = yes
				}
				add = 500
			}				
		}
	}	

	possible = {
		is_subject = no
		can_form_nation = GAV	
		NOT = { country_tier = empire }
	}

	weight = {
		value = 1000
	}
}

ai_strategy_unify_nagabumi = {
	icon = "gfx/interface/icons/ai_strategy_icons/unification.dds"
	
	type = diplomatic
	
	will_form_power_bloc = {
		always = yes
	}
	
	undesirable_infamy_level = {
		value = 90
	}
	
	unacceptable_infamy_level = {
		value = 100
	}
	
	ideological_opinion_effect_mult = {
		value = 0.5
	}
	
	wargoal_maneuvers_fraction = {
		value = 0.2
	}		
	
	diplomatic_play_neutrality = {
		value = 0
	}
	
	diplomatic_play_boldness = {
		value = 75
	}

	obligation_value = {
		value = 0
	
		if = {
			limit = { 
				scope:target_country = {			
					OR = {
						country_has_primary_culture = cu:gulf_naga
						country_has_primary_culture = cu:jungle_naga
					}
				}
			}
			add = 25
		}
	}	

	recklessness = {
		value = 0.5
	}
	
	aggression = {
		value = 0.25
		
		if = {
			limit = { 
				scope:target_country = {			
					OR = {
						country_has_primary_culture = cu:gulf_naga
						country_has_primary_culture = cu:jungle_naga
					}
				}
			}
			add = 2.75
		}

	}

	building_group_weights = {
		bg_army = 1.5
	}
	
	state_value = {
		if = {
			limit = {
				scope:target_state = {
					state_region = {
						OR = {
						is_homeland = cu:gulf_naga
						is_homeland = cu:jungle_naga
						}
					}
				}
				can_reach_target_state = yes					
			}
			add = 100
		}
	}
	
	wargoal_scores = {	
		unification_leadership = {
			value = 1000
		}	
	}
	
	secret_goal_scores = {
		protect = {		
			if = {
				limit = {
					scope:target_country = {
						OR = {
							country_has_primary_culture = cu:gulf_naga
							country_has_primary_culture = cu:jungle_naga
						}
						NOT = { is_unification_candidate = NBI }
						OR = { 
							is_supporting_unification_candidate = { who = root country_formation = NBI }
							is_subject_of = root
							is_in_customs_union_with = root
						}						
					}
				}
				add = 1000
			}		
		}		
		dominate = {		
			if = {
				limit = {
					scope:target_country = {
						OR = {
							country_has_primary_culture = cu:gulf_naga
							country_has_primary_culture = cu:jungle_naga
						}
						NOT = { is_unification_candidate = NBI }
						OR = { 
							is_supporting_unification_candidate = { who = root country_formation = NBI }
							is_subject_of = root
							is_in_customs_union_with = root
						}						
					}
				}
				add = 1000
			}		
		}
		conquer = {				
			if = {
				limit = {
					scope:target_country = {
						OR = {
							country_has_primary_culture = cu:gulf_naga
							country_has_primary_culture = cu:jungle_naga
						}
						NOR = { 
							is_subject_of = root
							is_in_customs_union_with = root
						}						
					}
					can_reach_target_country = yes
				}
				add = 500
			}				
		}
	}	

	possible = {
		is_subject = no
		can_form_nation = NBI	
		NOT = { country_tier = empire }
	}

	weight = {
		value = 1000
	}
}

ai_strategy_unify_atamzotayit = {
	icon = "gfx/interface/icons/ai_strategy_icons/unification.dds"
	
	type = diplomatic
	
	will_form_power_bloc = {
		always = yes
	}
	
	undesirable_infamy_level = {
		value = 90
	}
	
	unacceptable_infamy_level = {
		value = 100
	}
	
	ideological_opinion_effect_mult = {
		value = 0.5
	}
	
	wargoal_maneuvers_fraction = {
		value = 0.2
	}		
	
	diplomatic_play_neutrality = {
		value = 0
	}
	
	diplomatic_play_boldness = {
		value = 75
	}

	obligation_value = {
		value = 0
	
		if = {
			limit = { 
				scope:target_country = {			
					OR = {
						country_has_primary_culture = cu:rahoru
						country_has_primary_culture = cu:tqalt
						country_has_primary_culture = cu:waste_walker
						country_has_primary_culture = cu:yachaymanchi
					}
				}
			}
			add = 25
		}
	}	

	recklessness = {
		value = 0.5

		if = {
			limit = { 
				scope:target_country = {			
					OR = {
						country_has_primary_culture = cu:rahoru
						country_has_primary_culture = cu:tqalt
						country_has_primary_culture = cu:waste_walker
						country_has_primary_culture = cu:yachaymanchi
					}
				}
			}
			add = 2
		}

	}
	
	aggression = {
		value = 0.25
		
		if = {
			limit = { 
				scope:target_country = {			
					OR = {
						country_has_primary_culture = cu:rahoru
						country_has_primary_culture = cu:tqalt
						country_has_primary_culture = cu:waste_walker
						country_has_primary_culture = cu:yachaymanchi
					}
				}
			}
			add = 2.75
		}

	}

	building_group_weights = {
		bg_army = 1.5
	}
	
	state_value = {
		if = {
			limit = {
				scope:target_state = {
					state_region = {
						OR = {
							is_homeland = cu:rahoru
							is_homeland = cu:tqalt
							is_homeland = cu:waste_walker
							is_homeland = cu:yachaymanchi
						}
					}
				}
				can_reach_target_state = yes					
			}
			add = 100
		}
	}
	
	wargoal_scores = {	
		unification_leadership = {
			value = 1000
		}	
	}
	
	secret_goal_scores = {
		protect = {		
			if = {
				limit = {
					scope:target_country = {
						OR = {
							country_has_primary_culture = cu:rahoru
							country_has_primary_culture = cu:tqalt
							country_has_primary_culture = cu:waste_walker
							country_has_primary_culture = cu:yachaymanchi
						}
						OR = { 
							is_subject_of = root
							is_in_customs_union_with = root
						}						
					}
				}
				add = 1000
			}		
		}		
		dominate = {		
			if = {
				limit = {
					scope:target_country = {
						OR = {
							country_has_primary_culture = cu:rahoru
							country_has_primary_culture = cu:tqalt
							country_has_primary_culture = cu:waste_walker
							country_has_primary_culture = cu:yachaymanchi
						}
						OR = { 
							is_subject_of = root
							is_in_customs_union_with = root
						}						
					}
				}
				add = 1000
			}		
		}
		conquer = {				
			if = {
				limit = {
					scope:target_country = {
						OR = {
							country_has_primary_culture = cu:rahoru
							country_has_primary_culture = cu:tqalt
							country_has_primary_culture = cu:waste_walker
							country_has_primary_culture = cu:yachaymanchi
						}
						NOR = { 
							is_subject_of = root
							is_in_customs_union_with = root
						}						
					}
					can_reach_target_country = yes
				}
				add = 500
			}				
		}
	}	

	possible = {
		is_subject = no
		can_form_nation = TQL	
		NOT = { country_tier = empire }
	}

	weight = {
		value = 1000
	}
}

##############################