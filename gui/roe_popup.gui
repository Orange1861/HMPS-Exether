widget = {
    name = roe_popup_container
    allow_outside = yes
    size = { 100% 100% }
    layer = popups
    visible = "[GetMetaPlayer.GetPlayedOrObservedCountry.IsValid]"

    roe_domains_window = {}
}